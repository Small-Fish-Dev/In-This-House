@using Sandbox;
@using Sandbox.UI;

@namespace BrickJam
@inherits RootPanel
@attribute [StyleSheet("UI/Style.scss")]

<root>
</root>

@code {
    public static Hud Instance { get; private set; }

    public Hud()
    {
        Instance?.Delete(true);
        Instance = this;
    }
}
